import { Toaster } from "react-hot-toast";

const HotToast = () => {
    return (
        <Toaster
            position="top-center"
            reverseOrder={false}
        />
    );
};

export default HotToast;